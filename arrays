[]arrays
-fixed length
-same type
-indexable
-contiguous in memory

package main

import (
    "fmt"
)

func main() {
    var intArr [3]int32
    fmt.Println(intArr[0])
    fmt.Println(intArr[1:3])
}
